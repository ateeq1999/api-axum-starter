global:
  scrape_interval: 15s

scrape_configs:
  - job_name: api-starter-axum
    authorization:
      type: Bearer
      credentials: "VGAnpjESxBnoPjh/Gx6rIZwQJgoacl0unVrikxqGQzc="
    static_configs:
      # Requires METRICS_BIND_ADDR=0.0.0.0:9091 in the API's environment for local Docker use.
      - targets: ["host.docker.internal:9091"]
